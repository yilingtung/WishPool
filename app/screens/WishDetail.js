import React from 'react';
import { Text, View, ScrollView } from 'react-native';

const WishDetail = () => (
	<ScrollView>
		<View>
			<Text>WishDetail Page</Text>
		</View>
	</ScrollView>
);

export default WishDetail;
